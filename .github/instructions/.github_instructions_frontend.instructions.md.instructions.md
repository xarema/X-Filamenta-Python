---
applyTo: "**/*.html,**/*.css,**/*.js"
---

Frontend rules:

- Prefer HTMX patterns over custom JS when possible.
- Use Bootstrap 5 classes; custom CSS must use tokens (CSS variables) and stay minimal.
- Keep HTML semantic and accessible.
- No inline JS; prefer modules and `defer`.
